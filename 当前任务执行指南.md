# 当前任务执行指南

> 这是当前阶段唯一推荐先读的总入口。旧版拆分说明已放到 `文档归档/`，需要查历史时再看。

## 当前状态

02 数据组员已经提交：

- `02_11/class_distribution.csv`
- `02_11/微信图片_20260520235943_58_176.png`：类别分布图
- `02_11/微信图片_20260520235944_60_176.png`：样本 Mask 叠加图
- `02_11/数据分析结果记录模板.md`：数据分析文字记录

02 的结论可以用于报告/PPT，但有两个口径要注意：

1. `any=6666` 应表述为“当前有缺陷标注图片数为 6666”，不要直接写“所有训练图片都有缺陷”。
2. `train.csv` 最终统一为 `ImageId_ClassId + EncodedPixels` 标准格式。

## 当前每个人接下来做什么

| 角色 | 当前任务 | 看哪个文件 |
|---|---|---|
| 01 项目统筹 | 跟进 02 修正口径，收集 03 训练结果 | 本文档、`01_项目统筹与提交/team_task_board.md` |
| 02 数据分析 | 补充训练图片总数，确认是否存在无缺陷图片 | `02_11/数据分析结果记录模板.md` |
| 03 模型训练 | 按 02 结果跑 Debug、Baseline、submission | `03_模型训练与代码实现/03_模型训练统一指南.md` |
| 04 实验汇报 | 等 03 给结果后整理消融表和 PPT | `04_实验汇报与答辩/ablation_plan.md` |

## 02 需要补充给 03 的信息

02 还需要确认：

```text
train_images 总图片数：
有缺陷标注图片数：6666
无缺陷图片数：总图片数 - 6666
train.csv 当前格式：ImageId_ClassId + EncodedPixels / ImageId + ClassId + EncodedPixels
```

如果 `train.csv` 不是标准格式，03 会用转换脚本转成标准格式。

## 03 当前最重要的目标

03 不要先调参，先完成一条可运行闭环：

```text
标准 train.csv -> Debug 训练 -> Baseline 训练 -> submission.csv -> Kaggle 分数
```

03 完成后必须交付：

```text
训练平台：
训练耗时：
Val Dice：
Kaggle Public Score：
history.png：
val_predictions 示例图：
submission.csv：
遇到的问题：
```

## 文档如何使用

- 现在执行任务，只看 `当前任务执行指南.md` 和各自文件夹里的最新统一指南。
- `文档归档/` 里的内容不作为当前执行入口，只保留历史记录。
- 如果两个文档说法不一致，以本文件和 `03_模型训练统一指南.md` 为准。
