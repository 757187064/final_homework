# 03 执行清单：接 02 数据分析结果完成模型训练

> 你现在是 03 模型训练负责人。目标是基于 02 的数据分析，跑通训练、生成 Baseline 结果，并给 04 提供消融实验素材。

## 当前已知数据结论

02 已经完成：

- Class 1：897 张
- Class 2：247 张，样本最少
- Class 3：5150 张，样本最多
- Class 4：801 张
- 有缺陷图片统计：6666 张

注意：`6666` 更稳妥地解释为“当前有缺陷标注图片数”。是否存在无缺陷训练图片，要看 `train_images/` 总数。当前代码已支持把没有标注的图片补成全 0 Mask。

## 你现在的任务顺序

### 1. 进入代码目录

```bash
cd 03_模型训练与代码实现
```

### 2. 准备数据路径与标准标注格式

推荐目录：

```text
data/raw/
├── train.csv
├── sample_submission.csv
├── train_images/
└── test_images/
```

本项目统一使用 Kaggle 标准格式：

```text
ImageId_ClassId,EncodedPixels
```

如果你拿到的是 02 提到的分列格式：

```text
ImageId,ClassId,EncodedPixels
```

先转换：

```bash
python scripts/normalize_train_csv.py \
  --input data/raw/train.csv \
  --output data/raw/train_standard.csv
```

然后把配置文件里的：

```yaml
train_csv: train.csv
```

改成：

```yaml
train_csv: train_standard.csv
```

如果在 Kaggle Notebook 中运行，把配置文件里的：

```yaml
data:
  root: data/raw
```

改成 Kaggle 数据路径，例如：

```yaml
data:
  root: /kaggle/input/severstal-steel-defect-detection
```

### 3. 安装依赖

Kaggle Notebook：

```bash
pip install segmentation-models-pytorch albumentations opencv-python-headless
```

### 4. 先跑 Debug

```bash
python scripts/train.py --config configs/debug.yaml
```

你要看终端是否输出类似：

```text
数据统计：total=..., defect=..., empty=...
使用设备：cuda
epoch=1 train_loss=... val_loss=... val_dice=...
```

如果这里失败，不要继续跑 Baseline，先解决报错。

### 5. 跑 Baseline

```bash
python scripts/train.py --config configs/baseline.yaml
```

必须保留这些输出：

```text
outputs/baseline_unet_resnet34/
├── best_model.pth
├── config.yaml
├── history.csv
├── history.png
└── val_predictions/
```

### 6. 生成 Kaggle 提交

```bash
python scripts/infer.py \
  --config configs/baseline.yaml \
  --checkpoint outputs/baseline_unet_resnet34/best_model.pth \
  --output outputs/baseline_unet_resnet34/submission.csv
```

提交后记录 Public Score。

## 你要交给组长/04 的结果

```text
实验名：Baseline
配置文件：configs/baseline.yaml
训练平台：Kaggle GPU / 其他
训练耗时：
Val Dice：
Kaggle Public Score：
输出位置：
遇到的问题：
结论：
```

同时发这些文件或截图：

- `history.png`
- 若干 `val_predictions/*.png`
- Kaggle submission 分数截图
- 训练日志最后几行

## 基于 02 结果的训练注意点

- Class 2 样本极少，Baseline 可能对 Class 2 漏检严重。
- Class 3 样本最多，模型可能偏向多数类。
- 不要第一轮就乱改复杂模型，先得到可复现 Baseline。
- 后续优化优先尝试：数据增强、学习率调度、按类别阈值、类别权重或 loss 权重调整。
